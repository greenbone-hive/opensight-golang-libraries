![Greenbone Logo](https://www.greenbone.net/wp-content/uploads/gb_new-logo_horizontal_rgb_small.png)

# provider Package Documentation

This package holds the canonical value set for the systems assets are discovered
from: what an adapter connects to, and where a resource lives. Every service keys
off these values and this one validation instead of redefining the enum, so a
provider name never has to be translated between services.

The set is not limited to public clouds by design: an on-premises source becomes
a constant here alongside them rather than a parallel enum.

## Example Usage

```go
p := provider.Provider(adapter.Provider)
if !provider.Valid(p) {
	return fmt.Errorf("unknown provider %q", p)
}

for _, p := range provider.All {
	register(p)
}
```

---

<!-- gomarkdoc:embed:start -->

<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# provider

```go
import "github.com/greenbone/opensight-golang-libraries/pkg/provider"
```

## Index

- [Variables](<#variables>)
- [func Valid\(p Provider\) bool](<#Valid>)
- [type Provider](<#Provider>)


## Variables

<a name="All"></a>

```go
var All = []Provider{AWS, Azure, GCP, GreenboneAppliance}
```

<a name="Valid"></a>
## func [Valid](<https://github.com/greenbone/opensight-golang-libraries/blob/main/pkg/provider/provider.go#L18>)

```go
func Valid(p Provider) bool
```



<a name="Provider"></a>
## type [Provider](<https://github.com/greenbone/opensight-golang-libraries/blob/main/pkg/provider/provider.go#L7>)



```go
type Provider string
```

<a name="AWS"></a>

```go
const (
    AWS                Provider = "aws"
    Azure              Provider = "azure"
    GCP                Provider = "gcp"
    GreenboneAppliance Provider = "greenbone_appliance"
)
```

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)


<!-- gomarkdoc:embed:end -->